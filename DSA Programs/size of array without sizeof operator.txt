#include <stdio.h>

int main()
{
    int size,arr[100],n,i;
    printf("\nEnter the size..");
    scanf("%d",&n);
    printf("\nEnter the elements...");
    for(i=0;i<n;i++)
    {
        scanf("%d",&arr[i]);
         size= *(&arr + 1)-arr;
    }
   
    printf("\nArray size=%d",size*2);

    return 0;
}